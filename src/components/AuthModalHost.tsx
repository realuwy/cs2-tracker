// src/components/AuthModalHost.tsx
"use client";

// Temporary stub while migrating off Supabase auth.
// This prevents build errors from old signIn/signUp code.
export default function AuthModalHost() {
  return null;
}
