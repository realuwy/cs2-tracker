// src/components/auth-modal.tsx
"use client";

// Legacy Auth modal (Supabase) — intentionally no-op now.
// We use the new ID-based onboarding modal instead.
export default function AuthModal() {
  return null;
}

